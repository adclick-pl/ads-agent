node_modules/
.env
*.local
google-ads.yaml
# Account registry holds real client IDs — never commit it. Ship accounts.example.json instead.
accounts.json
.claude/accounts.json
*.csv
npm-debug.log*
.DS_Store

# Migawki drzewa grup plików PMax — cli.js zapisuje je w katalogu roboczym
# (listing-tree-<ID>-<data>.json) i zawierają realne ID konta klienta.
listing-tree-*.json
